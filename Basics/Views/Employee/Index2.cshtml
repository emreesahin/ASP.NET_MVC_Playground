@model String[]
<div>
    <h3 class = 'display-3'>
        Index2
    </h3>
</div>

<div class="lead">
    @foreach (string name in @Model)
    {
        <p>
            @name
        </p>
    }
</div>